---
---

<section id="inicio" class="hero">
  <div class="hero-background">
    <div class="hero-shapes">
      <div class="shape shape-1"></div>
      <div class="shape shape-2"></div>
      <div class="shape shape-3"></div>
    </div>
  </div>
  
  <div class="container">
    <div class="hero-content">
      <div class="hero-text">
        <h1 class="hero-title">
          Transformando vidas a través de la <span class="highlight">educación</span>
        </h1>
        <p class="hero-description">
          En Fundación DAADIOS trabajamos para que jóvenes estudiantes de secundaria con alto rendimiento académico puedan continuar sus estudios sin que las limitaciones económicas representen una barrera.
        </p>
        <div class="hero-buttons">
          <a href="#aplicar" class="btn btn-primary btn-large">Aplicar a Beca</a>
          <a href="#nosotros" class="btn btn-outline btn-large">Conoce Más</a>
        </div>
      </div>
      
      <div class="hero-image">
        <div class="hero-logo">
          <img src="/IMAGOTIPO_BLANCO.png" alt="Símbolo DAADIOS" class="symbol">
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    position: relative;
    overflow: hidden;
    background: linear-gradient(135deg, #F58220 0%, #FFC20E 100%);
    padding-left: 80px;
    padding-top: 12px;
    padding-right: 80px;
    box-sizing: border-box;
    width: 100%;
  }
  
  .hero-background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    overflow: hidden;
  }
  
  .hero-shapes {
    position: relative;
    width: 100%;
    height: 100%;
  }
  
  .shape {
    position: absolute;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.1);
    animation: float 6s ease-in-out infinite;
  }
  
  .shape-1 {
    width: 100px;
    height: 100px;
    top: 20%;
    right: 10%;
    animation-delay: 0s;
  }
  
  .shape-2 {
    width: 150px;
    height: 150px;
    top: 60%;
    left: 5%;
    animation-delay: 2s;
  }
  
  .shape-3 {
    width: 80px;
    height: 80px;
    top: 30%;
    left: 15%;
    animation-delay: 4s;
  }
  
  .hero-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--spacing-xxl);
    align-items: center;
    position: relative;
    z-index: 2;
  }
  
  .hero-title {
    font-size: 3.5rem;
    font-weight: 700;
    color: var(--color-white);
    margin-bottom: var(--spacing-lg);
    line-height: 1.2;
  }
  
  .highlight {
    color: var(--color-yellow);
    position: relative;
  }
  
  .highlight::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 3px;
    background: var(--color-yellow);
    border-radius: 2px;
  }
  
  .hero-description {
    font-size: 1.2rem;
    color: rgba(255, 255, 255, 0.9);
    margin-bottom: var(--spacing-xl);
    line-height: 1.6;
  }
  
  .hero-buttons {
    display: flex;
    gap: var(--spacing-md);
    flex-wrap: wrap;
  }
  
  .hero-image {
    display: flex;
    justify-content: center;
    align-items: center;
  }
  
  .hero-logo {
    width: 300px;
    height: 300px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 50%;
    backdrop-filter: blur(10px);
    border: 2px solid rgba(255, 255, 255, 0.2);
  }
  
  .symbol {
    width: 200px;
    height: auto;
  }
  
  @keyframes float {
    0%, 100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-20px);
    }
  }
  
  
  .btn-large {
    display: inline-block;
    padding: calc(var(--spacing-md) + 5px) calc(var(--spacing-xxl) + 5px);
    border: none;
    border-radius: var(--border-radius);
    font-family: var(--font-primary);
    font-weight: 600;
    text-decoration: none;
    text-align: center;
    cursor: pointer;
    transition: all 0.3s ease;
    font-size: 1.2rem;
    background: var(--color-yellow);
    color: var(--color-dark);
  }

  .btn-large:hover {
    background: var(--color-brown);
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
    text-decoration: none;
    color: var(--color-white);
  }

  @media (max-width: 768px) {
    .hero {
      padding-left: 6px;
      padding-right: 6px;
    }
    .hero-content {
      grid-template-columns: 1fr;
      gap: var(--spacing-xl);
      text-align: center;
    }
    .hero-title {
      font-size: 2.5rem;
    }
    .hero-description {
      font-size: 1.1rem;
    }
    .hero-logo {
      width: 250px;
      height: 250px;
    }
    .symbol {
      width: 150px;
    }
    .hero-buttons {
      justify-content: center;
    }
  }
</style>
